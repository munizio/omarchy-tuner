# Point SSH clients at the OpenSSH user agent. Changes apply on the next
# graphical session. Enable the socket separately:
#   systemctl --user enable --now ssh-agent.socket
export SSH_AUTH_SOCK="${XDG_RUNTIME_DIR}/ssh-agent.socket"
