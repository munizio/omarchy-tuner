# Ctrl+F anywhere in tmux (shell, nvim, agent) and prefix+f both run the
# picker. fzf --tmux draws the popup; do not wrap this in display-popup.
bind -N "Sessionizer" -n C-f run-shell -b "sessionizer"
bind -N "Sessionizer" f run-shell -b "sessionizer"
